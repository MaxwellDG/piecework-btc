import NotFound from '../../../(components)/notFound';

export default function TasksNotFound() {
    return (
        <NotFound text="Could not find any tasks associated with the given project id" />
    );
}
